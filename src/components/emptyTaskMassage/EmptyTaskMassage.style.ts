import styled from "styled-components";
import appUtils from "../../utils";

const MassageContainer = styled.div`
  margin-top: 4rem;
  margin: 3rem auto;
  border: 2px solid ${appUtils.colors.warmOrange};
  border-radius: 5px;
  width: 40%;
  padding: 1rem;
`;

const Styled = {
  MassageContainer,
};
export default Styled;
